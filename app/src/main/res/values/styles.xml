<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Стиль для сообщения, отправленного пользователем -->
    <style name="UserMessageStyle">
        <item name="android:background">@drawable/user_message_background</item>
        <item name="android:textColor">@color/user_message_text_color</item>
        <item name="android:gravity">end</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:maxWidth">300dp</item>
    </style>

    <!-- Стиль для сообщения, отправленного собеседником -->
    <style name="OtherMessageStyle">
        <item name="android:background">@drawable/other_message_background</item>
        <item name="android:textColor">@color/other_message_text_color</item>
        <item name="android:gravity">start</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:maxWidth">300dp</item>
    </style>
</resources>